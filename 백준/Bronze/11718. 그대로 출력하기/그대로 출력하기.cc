#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <cstring>

using namespace std;

int main(void) {
	string input;
	int index = 0;

	do {
		getline(cin, input);
		cout << input<<endl;
		if (input == "")
			break;
	} while (exit);


}
